name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main # Или ваша основная ветка, например 'master'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest # Используем Linux-окружение для сборки

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚡️
        uses: actions/setup-node@v4
        with:
          node-version: 20 # Или другая версия, которую вы используете

      - name: Install dependencies 📦
        run: npm ci # Используйте 'npm install' если нет lock-файла, но 'npm ci' предпочтительнее для CI

      - name: Build and export Next.js 🚀
        # Важно: В GitHub Actions (Linux-окружение) синтаксис VAR=value command работает
        env:
          NEXT_PUBLIC_BASE_PATH: /${{ github.event.repository.name }} # Автоматически подставляет имя репозитория
        run: npm run build # Эта команда сгенерирует статику в 'out'

      - name: Add .nojekyll file 📄
        run: touch out/.nojekyll # Создаем .nojekyll для GitHub Pages

      - name: Deploy to GitHub Pages 🚀
        uses: peaceiris/actions-gh-pages@v4 # Используем action для деплоя
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./out # Указываем, откуда брать файлы для публикации
          # publish_branch: gh-pages # Если вы хотите деплоить в ветку gh-pages, раскомментируйте это
          # Но если вы настроите GitHub Pages на Source: Deploy from a branch -> main -> /out, то это не нужно
